using System;
using PowerDev.Enterprise.Eureka.Extensoes;
using PowerDev.Enterprise.Eureka.Utils;

namespace PowerDev.Enterprise.Eureka.Conversores
{
    public class DataJsonConversor //: IsoDateTimeConverter
    {
        //public override void WriteJson(JsonWriter writer, object value, JsonSerializer serializer)
        //{
        //    if (value == null || ConverteUtils.sempreConverteDate(value) == DateTime.MinValue)
        //    {
        //        writer.WriteValue("");
        //        return;
        //    }
        //    base.WriteJson(writer, new DateTimeOffset(value.como<DateTime>()), serializer);
        //}

        //public override object ReadJson(JsonReader reader, Type objectType, object existingValue, JsonSerializer serializer)
        //{
        //    var o = base.ReadJson(reader, objectType, existingValue, serializer);
        //    return o;
        //}
    }
}